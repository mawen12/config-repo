tracer:
   property: "I AM THE PROD"

spring:
   datasource:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://localhost:3306/test?serverTimezone=UTC&verifyServerCertificate=false&useSSL=false
      username: root
      password: admin
      testWhileIdle: true
      validationQuery: "SELECT 1"
   jpa:
      show-sql: true